#include "monty.h"

/**
 * nop - doesn't do anything
 * @stack: double pointer to the beginning of the stack
 *
 * Description: The opcode nop doesn’t do anything.
 */
void nop(stack_t **stack)
{
    (void)stack; /* Do nothing */
}
